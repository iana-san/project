"use strict";

const category = 'toys';

console.log(`https://someurl.com/${category}/5`);

if (count > max_count) {
    max_count = count;
    maxfreq = arr[i];
    console.log(maxfreq);
 }